import type { ProvenanceManifestEntry } from "../../schema/index.js";

/** TODO(wave-2): append phase entries to manifest.yaml on record_phase */
export async function appendManifestEntry(
  _taskDir: string,
  _entry: ProvenanceManifestEntry,
): Promise<void> {
  // TODO(wave-2)
}
